# 📱 Mobile Automation Tests - Alfa-Test App

Автоматизированное тестирование мобильного приложения **Alfa-Test** с использованием **Java + Appium**.

---

## 🎯 О проекте

- Автоматизация мобильного приложения Android
- Использование паттерна Page Object Model
- Работу с XPath локаторами
- Параметризация тестов
- Явные ожидания (WebDriverWait)
- Поиск и документирование багов

---

## 🛠 Технологии

- **Java 11**
- **Appium** (UiAutomator2)
- **JUnit 5** (параметризация)
- **Maven**
- **Selenium WebDriver**

---

## 📁 Структура проекта

```
src/
├── main/java/com/alfabank/tests/
│   ├── config/
│   │   └── TestConfig.java          # Конфигурация
│   ├── pages/
│   │   ├── BasePage.java            # Базовые методы
│   │   └── LoginPage.java           # Page Object логина
│   └── utils/
│       └── DriverFactory.java       # Управление драйвером
└── test/java/com/alfabank/tests/
    ├── base/
    │   └── BaseTest.java            # Базовый класс тестов
    └── tests/
        └── LoginTests.java          # Автотесты авторизации
```

---

## ✅ Реализованные тесты

### Тест 1: Успешная авторизация ✅
Проверка входа с валидными данными (Login / Password)

### Тест 2: Ограничение длины логина ❌ БАГ
**Требование:** Максимум 50 символов  
**Фактически:** Принимает 60+ символов  
**Статус:** Тест падает - найден баг

### Тест 3: Недопустимые символы (параметризованный) ❌ БАГ
**Требование:** Только латинские буквы, показывать ошибку "InvalidValue"  
**Фактически:** Нет валидации, нет сообщения об ошибке  
**Статус:** 5 запусков падают - найден баг  
**Проверяемые значения:**
- Русские буквы: "Тест123", "логин"
- Спецсимволы: "@", "#", "$"

### Тест 4: Кнопка показа/скрытия пароля ✅
Проверка работы "глазика" - нечётное нажатие показывает, чётное скрывает

### Тест 5: Доступность кнопки после скрытия клавиатуры ✅
Демонстрация решения проблемы: клавиатура закрывала кнопку "Войти"

---

## 🐛 Найденные баги

### БАГ #1: Нет ограничения длины логина
- **Приоритет:** MEDIUM
- **Требование:** "максимальное количество символов для ввода - 50"
- **Фактически:** Поле принимает 60+ символов
- **Тест:** `testLoginMaxLength()`

### БАГ #2: Нет валидации недопустимых символов
- **Приоритет:** HIGH (проблема безопасности)
- **Требование:** "При вводе неразрешенных символов необходимо отображать сообщение InvalidValue"
- **Фактически:** Нет валидации входных данных, нет сообщения об ошибке
- **Тест:** `testInvalidCharacters()` (параметризованный)
- **Риски:** Возможность SQL-injection, XSS

---

## 🎨 Использованные паттерны

### 1. Page Object Model (POM)
Каждая страница приложения - отдельный класс с локаторами и методами

### 2. Factory Pattern
Централизованное создание драйвера через `DriverFactory`

### 3. Наследование
Общая логика в базовых классах (`BasePage`, `BaseTest`)

---

## 🔧 Технические решения

### Локаторы
- **XPath:** Основные элементы (гибкие)
- **CSS:** Альтернативные локаторы через ID (быстрые)

### Ожидания
**WebDriverWait вместо Thread.sleep:**
```java
wait.until(ExpectedConditions.elementToBeClickable(loginButton));
```

### Решение проблемы с клавиатурой
```java
public void clickLoginButton() {
    hideKeyboard();  // Решение найдено через отладку
    wait.until(ExpectedConditions.elementToBeClickable(loginButton));
    clickElement(loginButton);
}
```

### Параметризация
```java
@ParameterizedTest
@ValueSource(strings = {"Тест123", "user@mail.ru", "test#user"})
public void testInvalidCharacters(String invalidLogin) { ... }
```

---

## 🚀 Как запустить

### Предварительные требования:
1. Java 11+
2. Maven
3. Appium Server
4. Android Emulator или реальное устройство
5. APK приложения Alfa-Test

### Установка:
```bash
# Клонировать репозиторий
git clone https://github.com/YOUR_USERNAME/qa-mobile-tests.git
cd qa-mobile-tests

# Установить зависимости
mvn clean install
```

### Настройка:
1. Запустить Appium Server:
```bash
appium
```

2. Запустить Android Emulator или подключить устройство

3. Указать путь к APK в `TestConfig.java`:
```java
public static final String APP_PATH = "ВАШ_ПУТЬ/app-debug.apk";
```



